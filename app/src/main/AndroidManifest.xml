<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.innovativebanking">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_red"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activities.UpdateVaultActivity"></activity>
        <activity android:name=".activities.CreateVaultActivity" />
        <activity android:name=".activities.VaultsActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/map_key" />

        <activity android:name=".activities.StatisticsActivity" />
        <activity android:name=".activities.UpdateUserDetails" />
        <activity android:name=".activities.UpdateCardDetails" />
        <activity android:name=".activities.InvestActivity" />
        <activity android:name=".activities.AddCardActivity" />
        <activity android:name=".activities.AddMoneyActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.InfoActivity" />
        <activity android:name=".activities.SendMoneyActivity" />
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity" />
    </application>

</manifest>